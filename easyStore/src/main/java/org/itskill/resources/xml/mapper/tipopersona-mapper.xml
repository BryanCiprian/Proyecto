<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="org.itskill.mapper.TipoPersonaMapper">

 <select id="listarAll" resultType="TipoPersonaBean">
	    SELECT id_tipo_persona as idTipoPersona, desc_tipo_persona as descTipoPersona
        FROM tbl09_tipo_persona where id_estado=1 order by 2;
 	</select>
 	     
</mapper>